import * as React from 'react';
import {Text, View} from 'react-native';

interface TextInputAtomProps {}

export const TextInputAtom: React.FC<TextInputAtomProps> = ({}) => {
  return (
    <View>
      <Text>TextInputAtom</Text>
    </View>
  );
};
